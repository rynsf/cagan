# Training Module

The training scaffold source now lives in this folder.

Primary docs:
- `../../docs/training.md`

Build (compatibility command):

```bash
sh build_train.sh
```

This delegates to `scripts/build_training.sh` and outputs binaries to `bin/`.
